
# listValues = [];
listValues = [4,6,6,8,4,4,4,7,7,8];

maxMode = 0;

def parser(l,m):
    for i in range(10):
        l.append(i)
    print(l)
    for i in (len(l)):
        for j in (j[i],len(l))
            if(l[i]==):

parser(listValues,maxMode)
